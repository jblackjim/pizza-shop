import { PizzaPlanetValidators } from './pizza-planet-validators';

describe('PizzaPlanetValidators', () => {
  it('should create an instance', () => {
    expect(new PizzaPlanetValidators()).toBeTruthy();
  });
});
